public class Main {
    
    public static void main(String[] args) {

        /*
        JFrame frame = new JFrame("Java Swing GUI");
        // or on a seperate line
        // frame.setTitle("Java Swing GUI"); 
        
        frame.setSize(800, 600);

        frame.setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);   // close the application when the frame is closed
        // by default it is set to HIDE_ON_CLOSE which hides the frame but the application is still running
        
        frame.setResizable(false);  // true by default
        frame.setVisible(true);     // required to display the frame

        ImageIcon icon = new ImageIcon("./github_logo.png");
        frame.setIconImage(icon.getImage());    // change frame's icon

        frame.getContentPane().setBackground(new Color(0x123456));  // change background color of the frame (hex, rgb, Color.colorname)
        */

        // MyFrame myFrame = new MyFrame();
        new MyFrame();
    }
}
